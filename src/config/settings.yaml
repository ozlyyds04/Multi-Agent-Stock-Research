project:
  name: "Automated Stock Market Research Team"
  version: "0.1.0"

orchestration:
  timeout_sec: 170
  max_news: 9
  price_days_default: 5

StrictMode:
  strict_mode: true

llm:
  provider: "deepseek"         # 当前默认：DeepSeek；可切回 "openai"（官方 OpenAI）
  model: "deepseek-v4-flash"
  temperature: 1

report:
  outdir: "artifacts"
  filename_template: "{{symbol}}_{{date}}_report.md"

compliance:
  forbidden_phrases:
    - "guaranteed returns"
    - "cannot go down"
    - "inside information"
    - "guaranteed returns"
    - "cannot go down"
    - "inside information"
    - "sure shot"
    - "risk free"
    - "100% profit"
  disclosure:
    - "This report is for informational purposes only and not investment advice."
    - "Data sources: Alpha Vantage, AKShare, public RSS feeds."
    - "Past performance does not guarantee future results."

news:
  sources:
    - "https://feeds.finance.yahoo.com/rss/2.0/headline?s={{symbol}}&region=US&lang=en-US"
    - "https://www.benzinga.com/feeds/pro/news/{{symbol}}"   # Some may fail; tool handles fallback
